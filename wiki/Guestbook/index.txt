= Guestbook/

This is currently the only editable page.

*Unicode* 你好 from A8 China.

this is _version 20260511_

BTW config.py is working

this is _version 20260512_

Should you be *worried* about getting stuck behind a NAT?

(i forgot to restart lol)

We are back! :D

    push test.....
    [x2 with PATH in `start_pre']
    [x3 with PATH in `${name}_env'...]

manual URL resolution worked!
(thx claude!!!)

Hi from my phone 📱

(oh and we should totally include the UA so
we know what OS everyone's using too...)

The food is good

okay but Todo

    * bump version (autoreconf -i)
    * float currently broken on mobile; could be nice to use flex box
         - add some goddamn margin below <main>???, gosh this looks stupid
    * api health; upptime on the 472 account
    * swagger docs look stupid; fix those
    * unsanitized interpolation in error route (check???
         - this is exactly how it broke for netifrc.
           we don't wanna fall into the same trap)
    * sign our parser.js with SHA384 on build
    * tighter CORS
    * wire ACL
    * OIDC; /authz (gh pages); /login /logout (engine)
         - identity by domain (how does tailscale do it??)
           or identity by localpart@domain
         - ... do we need to be stateful
    * privacy policy if that's worth adding (but there
      won't be any cookies on GitHub pages, so)

my iphone 6+ couldn't make it; could be nice to
add a disclaimer that we run on very (intentionally)
bleeding-edge JS/HTML functionalities

oh and have i mentioned the readme is totally wrong

    https://github.com/CS272-S26/p11/blob/main/README.md

FIX IT!!!1!!!!
